# Anonymization checklist

- Remove bodies, credentials, tokens, customer names, private URLs, and IDs.
- Replace identifiers with deterministic synthetic values and example.invalid locators.
- Keep inputs separate from labels under ignored paths.
- Confirm no secrets remain; reject rather than redact when uncertain.
