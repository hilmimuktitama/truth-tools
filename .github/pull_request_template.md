## Summary

<!-- One or two sentences: what changes and why. -->

## Evidence

<!-- For review-behavior changes: the exact input shape, the review outcome before and after, and which test or evaluation case proves it. -->

## Checks

- [ ] `npm test` passes
- [ ] `npm run check` passes
- [ ] `npm run contracts:verify` passes (schema changes)
- [ ] `npm run demo` passes (fixtures, demo data, or dist changes)
- [ ] `npm run eval` passes (review-policy changes)
- [ ] `npm pack --dry-run` inspected for allowlisted contents
- [ ] No raw source bodies, credentials, or secrets introduced

## Related

<!-- Issue or ADR links, if any. -->
